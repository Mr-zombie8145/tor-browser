# vim: filetype=yaml sw=2
version: '[% c("abbrev") %]'
git_url: https://github.com/uber-go/atomic
version: 1.3.2
git_hash: 'v[% c("version") %]'
filename: '[% project %]-[% c("version") %]-[% c("var/osname") %]-[% c("var/build_id") %].tar.gz'
# XXX: 1.3.2 is the first signed tag, use the GPG stuff

build: '[% c("projects/go/var/build_go_lib") %]'

var:
  container:
    use_container: 1
  go_lib: go.uber.org/atomic

input_files:
  - project: container-image
  - name: go
    project: go
